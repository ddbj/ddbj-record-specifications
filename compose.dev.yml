services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ddbj-record-specifications-dev
    container_name: ddbj-record-specifications-dev
    volumes:
      - ${PWD}:/app
    working_dir: /app
    command: [ "sleep", "infinity" ]
    init: true
